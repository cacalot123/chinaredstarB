/**
 *page grid view
 **/
define('js/modules/view/layout', [],
    function(

    ) {
        var LayoutView = Backbone.View.extend({
            initialize: function(options, config) {
                var t = this;
            },
            render: function() {
                // body...
                console.log('123');
            }
        });
        return LayoutView;
    });